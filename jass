"use client";

import Link from "next/link";
import { useState } from "react";

export default function Home() {
  const [busy, setBusy] = useState<null | "login" | "register">(null);

  const lock = (key: "login" | "register") => {
    // Evita toques repetidos
    if (busy) return;
    setBusy(key);
  };

  return (
    <div className="relative min-h-screen overflow-hidden text-slate-900">
      {/* Fondo premium con gradientes + textura + movimiento */}
      <div className="absolute inset-0 bg-gradient-to-br from-rose-200 via-white to-sky-200" />
      <div className="absolute inset-0 opacity-70 [background:radial-gradient(circle_at_20%_20%,rgba(255,0,128,.18),transparent_45%),radial-gradient(circle_at_80%_30%,rgba(0,140,255,.18),transparent_45%),radial-gradient(circle_at_40%_90%,rgba(125,70,255,.14),transparent_50%)]" />

      {/* “Auroras” en movimiento */}
      <div className="absolute -top-24 -left-24 h-72 w-72 rounded-full bg-rose-400/30 blur-3xl animate-floatSlow" />
      <div className="absolute top-24 -right-24 h-80 w-80 rounded-full bg-sky-400/30 blur-3xl animate-floatSlow2" />
      <div className="absolute -bottom-24 left-1/3 h-96 w-96 rounded-full bg-violet-400/25 blur-3xl animate-floatSlow3" />

      {/* Brillos sutiles */}
      <div className="absolute inset-0 opacity-40 bg-[linear-gradient(to_right,rgba(255,255,255,.5)_1px,transparent_1px),linear-gradient(to_bottom,rgba(255,255,255,.35)_1px,transparent_1px)] bg-[size:60px_60px]" />

      <main className="relative z-10 flex items-center justify-center p-6 md:p-10">
        <div className="w-full max-w-5xl">
          {/* Encabezado superior */}
          <div className="mb-6 flex items-center justify-between gap-3">
            <div className="inline-flex items-center gap-2 rounded-full bg-white/65 backdrop-blur border border-white/60 px-4 py-2 shadow-sm">
              <span className="h-2 w-2 rounded-full bg-emerald-500 animate-pulse" />
              <span className="text-sm font-semibold text-slate-800">
                Comunidad • Conexión • Valores
              </span>
            </div>

            <div className="hidden sm:flex items-center gap-2 text-xs text-slate-700">
              <span className="rounded-full bg-white/65 backdrop-blur border border-white/60 px-3 py-1 shadow-sm">
                Premium UI
              </span>
              <span className="rounded-full bg-white/65 backdrop-blur border border-white/60 px-3 py-1 shadow-sm">
                SUD vibe ✨
              </span>
            </div>
          </div>

          {/* Tarjeta principal */}
          <div className="relative rounded-[28px] bg-white/70 backdrop-blur-xl border border-white/60 shadow-[0_20px_70px_rgba(0,0,0,.12)] overflow-hidden">
            {/* Borde brillante */}
            <div className="pointer-events-none absolute inset-0 rounded-[28px] [mask-image:linear-gradient(#000,transparent)]">
              <div className="absolute -inset-1 bg-gradient-to-r from-rose-400/40 via-violet-400/35 to-sky-400/40 blur-2xl" />
            </div>

            <div className="grid md:grid-cols-2">
              {/* Columna izquierda */}
              <div className="p-8 md:p-10">
                <h1 className="text-5xl md:text-6xl font-black tracking-tight">
                  <span className="bg-clip-text text-transparent bg-gradient-to-r from-slate-900 via-slate-900 to-slate-700">
                    JASSAPP
                  </span>
                </h1>

                <p className="mt-3 text-slate-700 text-base md:text-lg leading-relaxed">
                  Un espacio juvenil, respetuoso y con propósito para conocer personas
                  con tus mismos valores.
                </p>

                {/* “Detalles SUD” suaves (sin ser cargoso) */}
                <div className="mt-5 flex flex-wrap gap-2">
                  <Badge>Respeto primero</Badge>
                  <Badge>Match con propósito</Badge>
                  <Badge>Perfil auténtico</Badge>
                  <Badge>Privacidad</Badge>
                </div>

                {/* Botones con anti doble-click + “Espere por favor...” */}
                <div className="mt-7 flex flex-col sm:flex-row gap-3">
                  <Link
                    href="/login"
                    onClick={() => lock("login")}
                    aria-disabled={busy !== null}
                    className={[
                      "relative inline-flex items-center justify-center rounded-2xl px-6 py-4 font-bold text-white transition",
                      "shadow-[0_14px_35px_rgba(15,23,42,.25)]",
                      busy
                        ? "bg-slate-900/60 cursor-not-allowed"
                        : "bg-slate-900 hover:translate-y-[-1px] hover:bg-slate-800 active:translate-y-[0px]",
                    ].join(" ")}
                  >
                    <span className="absolute inset-0 rounded-2xl bg-gradient-to-r from-rose-500/25 via-violet-500/20 to-sky-500/25 opacity-0 hover:opacity-100 transition" />
                    <span className="relative">
                      {busy === "login" ? "Espera por favor…" : "Iniciar sesión"}
                    </span>
                  </Link>

                  <Link
                    href="/register"
                    onClick={() => lock("register")}
                    aria-disabled={busy !== null}
                    className={[
                      "inline-flex items-center justify-center rounded-2xl px-6 py-4 font-bold transition",
                      "border border-white/70 bg-white/60 backdrop-blur hover:bg-white/75",
                      busy ? "cursor-not-allowed opacity-70" : "",
                    ].join(" ")}
                  >
                    {busy === "register" ? "Espera por favor…" : "Crear cuenta"}
                  </Link>
                </div>

                {/* Mini aviso */}
                <div className="mt-3 text-xs text-slate-600">
                  Tip: Los botones se bloquean para evitar toques repetidos ✅
                </div>

                {/* Features premium */}
                <div className="mt-7 grid grid-cols-2 gap-3">
                  <Feature
                    title="Diseño Premium"
                    desc="Interfaz moderna y llamativa."
                    icon="✨"
                  />
                  <Feature
                    title="Ambiente sano"
                    desc="Reglas claras y respeto."
                    icon="🛡️"
                  />
                  <Feature
                    title="Matches"
                    desc="Solo si ambos se gustan."
                    icon="💞"
                  />
                  <Feature
                    title="Chat"
                    desc="Conversación con propósito."
                    icon="💬"
                  />
                </div>

                <div className="mt-8 text-xs text-slate-600">
                  Creador:{" "}
                  <span className="font-bold text-slate-800">Bryan Carrión</span>
                </div>
              </div>

              {/* Columna derecha */}
              <div className="relative p-8 md:p-10 flex items-center justify-center bg-gradient-to-br from-white/30 to-white/0">
                {/* Elementos decorativos animados */}
                <div className="absolute -top-10 left-10 h-24 w-24 rounded-full bg-rose-400/25 blur-xl animate-bob" />
                <div className="absolute bottom-12 -right-10 h-28 w-28 rounded-full bg-sky-400/25 blur-xl animate-bob2" />

                {/* Tarjeta del logo */}
                <div className="w-full max-w-sm">
                  <div className="rounded-[26px] border border-white/70 bg-white/70 backdrop-blur shadow-lg p-6">
                    <div className="relative">
                      {/* Halo */}
                      <div className="absolute -inset-2 rounded-3xl bg-gradient-to-r from-rose-400/25 via-violet-400/20 to-sky-400/25 blur-2xl animate-glow" />
                      <img
                        src="/logo.jpg"
                        alt="Logo JASSAPP"
                        className="relative w-full rounded-2xl shadow-md"
                      />
                    </div>

                    <div className="mt-5 text-center">
                      <div className="text-sm font-extrabold text-slate-900">
                        JASSAPP está funcionando 🚀
                      </div>
                      <div className="text-xs text-slate-600 mt-1">
                        Listo para construir el tinder SUD
                      </div>
                    </div>

                    {/* “SUD vibe” elegante */}
                    <div className="mt-5 flex items-center justify-center gap-2 text-xs text-slate-700">
                      <span className="inline-flex items-center gap-1 rounded-full bg-white/70 border border-white/70 px-3 py-1">
                        🤍 Valores
                      </span>
                      <span className="inline-flex items-center gap-1 rounded-full bg-white/70 border border-white/70 px-3 py-1">
                        🙏 Respeto
                      </span>
                      <span className="inline-flex items-center gap-1 rounded-full bg-white/70 border border-white/70 px-3 py-1">
                        ✨ Propósito
                      </span>
                    </div>
                  </div>

                  <div className="mt-4 text-center text-xs text-slate-700/80">
                    “Conecta con fe, respeto y buena vibra.”
                  </div>
                </div>
              </div>
            </div>
          </div>

          {/* Footer */}
          <div className="mt-6 text-center text-xs text-slate-700/80">
            © {new Date().getFullYear()} JASSAPP • Creador: Bryan Carrión
          </div>
        </div>
      </main>

      {/* Animaciones (Tailwind custom) */}
      <style jsx global>{`
        @keyframes floatSlow {
          0% { transform: translate(0, 0); }
          50% { transform: translate(18px, 12px); }
          100% { transform: translate(0, 0); }
        }
        @keyframes floatSlow2 {
          0% { transform: translate(0, 0); }
          50% { transform: translate(-16px, 10px); }
          100% { transform: translate(0, 0); }
        }
        @keyframes floatSlow3 {
          0% { transform: translate(0, 0); }
          50% { transform: translate(10px, -14px); }
          100% { transform: translate(0, 0); }
        }
        @keyframes bob {
          0% { transform: translateY(0); }
          50% { transform: translateY(-10px); }
          100% { transform: translateY(0); }
        }
        @keyframes bob2 {
          0% { transform: translateY(0); }
          50% { transform: translateY(12px); }
          100% { transform: translateY(0); }
        }
        @keyframes glow {
          0% { opacity: 0.55; }
          50% { opacity: 0.9; }
          100% { opacity: 0.55; }
        }

        .animate-floatSlow { animation: floatSlow 10s ease-in-out infinite; }
        .animate-floatSlow2 { animation: floatSlow2 11s ease-in-out infinite; }
        .animate-floatSlow3 { animation: floatSlow3 12s ease-in-out infinite; }
        .animate-bob { animation: bob 5s ease-in-out infinite; }
        .animate-bob2 { animation: bob2 6s ease-in-out infinite; }
        .animate-glow { animation: glow 4.5s ease-in-out infinite; }
      `}</style>
    </div>
  );
}

function Badge({ children }: { children: React.ReactNode }) {
  return (
    <span className="inline-flex items-center rounded-full bg-white/70 backdrop-blur border border-white/70 px-3 py-1 text-xs font-semibold text-slate-800 shadow-sm">
      {children}
    </span>
  );
}

function Feature({
  title,
  desc,
  icon,
}:
